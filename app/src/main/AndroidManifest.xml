<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matsfjeld.valutta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".order"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".datePicker"
            android:parentActivityName=".order"/>
        <activity android:name=".preOverview"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".travelOverview"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".returnOverview"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".smartAmount"
            android:parentActivityName=".order"></activity>

    </application>

</manifest>